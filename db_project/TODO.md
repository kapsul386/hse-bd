# Финальный статус готовности проекта

Дата последней сборки: 2026-06-12.

## Закрыто в финальной версии

- [x] DDL синхронизирован до `schema.sql` v4.
- [x] О13 «адрес доставки принадлежит клиенту заказа» закрыт составным FK `customer_order(address_id, customer_id) -> address(address_id, customer_id)`.
- [x] ФТ-12 «оценка заказа» покрыта таблицей `order_review`, запросом Q13 и аналитикой Q14.
- [x] О24 «способ оплаты принимается рестораном» закрыт декларативно через `payment.restaurant_id` и FK `payment(restaurant_id, method) -> restaurant_payment_method(restaurant_id, method)`.
- [x] Временные инварианты заказа и оплаты закрыты CHECK-ами `chk_order_time_flow`, `chk_order_status_time`, `chk_order_courier_status`, `chk_payment_paid_at`.
- [x] T1 считает промокод, проверяет срок действия, минимальную сумму и полноту вставленных позиций.
- [x] Q4 показывает сумму позиций до скидки и фактическую оплату.
- [x] Q8 исключает отменённые заказы из продаж.
- [x] T2 не отменяет уже доставленные или уже отменённые заказы.
- [x] `schema.dbml`, `seed.sql`, `dml.sql`, `decisions.md`, `ai_log.md` и итоговый документ синхронизированы с v4.
- [x] `er_diagram.png` переэкспортирована и содержит `payment.restaurant_id`.
- [x] Разделы 13–15 итогового документа заполнены: фрагменты основной AI-модели, независимая верификация второй моделью, комментарий команды.
- [x] Раздел 16 итогового документа оформлен на основе `decisions.md`.

## Проверено локально

- [x] На PostgreSQL 18.3 из Postgres.app применяются `schema.sql` и `seed.sql`.
- [x] Загружаются контрольные данные: 9 заказов, 9 оплат, 5 отзывов.
- [x] Прямой `INSERT INTO payment` с методом оплаты, который ресторан не принимает, отвергается внешним ключом.
- [x] Заказ с `paid_at < created_at` отвергается CHECK-ограничением.
- [x] T1 с промокодом `WELCOME200` на сумму позиций 1220.00 создаёт оплату 1020.00.

## Перед сдачей вручную

- [ ] Подставить фактические ФИО участников вместо нейтральных обозначений «Участник 1–4» в разделе 1.
- [ ] При экспорте в PDF/DOCX приложить или встроить полные листинги `schema.sql` и `dml.sql`, если преподаватель требует строго один файл.
