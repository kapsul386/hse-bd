# TODO по ролям — итоги аудита 2026-06-10

Статус проверки: `schema.sql` + `seed.sql` + Q8–Q12 + T1/T2 **прогнаны на PostgreSQL 18.4 в Docker — работают**, выводы Q9/Q11/Q12 совпадают с заявленными в документе. Ниже — что аудит нашёл и кто чинит. Найденные дыры №1–3 подтверждены негативными тестами (вставки прошли без ошибок).

> **Обновление 2026-06-10 (вечер):** команда одобрила топ-5 быстрых правок — применены (схема v2, dml v1.2). Негативные тесты перепрогнаны: дыры «категория чужого ресторана» и «percent > 100» теперь отвергаются БД; Q8 без отменённых; T1 работает на v2. Открытыми остаются: О13 (адрес чужого клиента), ФТ-12, О24 и пополнение `ai_log.md`.

Пометка ⚠️ schema-freeze: изменения `schema.sql` вносит только Роль 1 (по правилам репозитория) — снипеты ниже готовы к применению, но решение за архитектором.

---

## Роль 1 — Архитектор схемы

- [x] ~~Дыра: блюдо может ссылаться на категорию чужого ресторана~~ — **закрыто в v2** (2026-06-10, одобрено командой): `UNIQUE (category_id, restaurant_id)` на `menu_category` + составной FK из `menu_item`, прямо в `CREATE TABLE`. Негативный тест теперь падает с ошибкой FK (перепроверено на PG 18.4). `schema.dbml` синхронизирован, §10 документа дополнен.
- [ ] ⚠️ **Дыра: заказ может ссылаться на адрес чужого клиента (О13).** Подтверждено тестом. Раздел 6 утверждает «простым FK не выразить» — верно, но **составным** FK выразить можно (тот же приём, что в `order_item`):
  ```sql
  ALTER TABLE address ADD UNIQUE (address_id, customer_id);
  ALTER TABLE customer_order DROP CONSTRAINT customer_order_address_id_fkey;
  ALTER TABLE customer_order ADD FOREIGN KEY (address_id, customer_id)
      REFERENCES address (address_id, customer_id);
  ```
  После правки обновить О13 в разделе 6 (декларативно, а не «приложение/триггер»). Если решите НЕ делать — это готовый кандидат в раздел 16 (спорное решение: почему оставили процедурно).
- [ ] **Решить судьбу ФТ-12 (оценка заказа/рейтинг):** в схеме нет ни одной таблицы под рейтинги, требование висит непокрытым — прямой удар по рубрике №2 («соответствие DDL требованиям»). Либо добавить сущность `order_review`, либо вычеркнуть ФТ-12 из раздела 3. Решение командное, зафиксировать в `decisions.md`.
- [ ] Экспортировать PNG из dbdiagram.io (для PDF-версии документа Mermaid не отрендерится). `schema.dbml` под v2 уже синхронизирован аудитом; Mermaid в §5 структурных изменений не требует (связь категория→блюдо уже нарисована).

## Роль 2 — Аналитик зависимостей и нормализации

- [x] ~~Слабый CHECK промокода (percent = 150 % вставлялся)~~ — **закрыто в v2** (2026-06-10, одобрено командой): `CHECK (discount_type <> 'percent' OR discount_value <= 100)` в `CREATE TABLE promo_code`. Негативный тест падает с ошибкой CHECK, корректный percent=15 проходит (перепроверено).
- [ ] **О23 (`created_at ≤ paid_at ≤ delivered_at`) помечено «приложение/триггер», но это однострочные CHECK** — внутристрочные сравнения выразимы декларативно:
  ```sql
  ALTER TABLE customer_order ADD CONSTRAINT chk_order_times CHECK (
      (paid_at      IS NULL OR paid_at      >= created_at) AND
      (delivered_at IS NULL OR (paid_at IS NOT NULL AND delivered_at >= paid_at)));
  ```
- [ ] **Упущенное текстовое ограничение:** способ оплаты заказа должен входить в принимаемые рестораном (`payment.method ∈ restaurant_payment_method` ресторана заказа). В списке О1–О23 его нет. Добавить как О24 (обеспечение: приложение/T1 — или составной FK через `payment.restaurant_id`, обсудить с Ролью 1).
- [ ] **Согласованность `payment.status='paid'` ⇒ `paid_at IS NOT NULL`** — дешёвый CHECK, сейчас отсутствует.
- [ ] Перепроверить раздел 7.1 после правок Роли 1: если категория↔ресторан станет декларативной, формулировку «значение всегда согласовано» можно усилить ссылкой на FK.

## Роль 3 — SQL-разработчик

- [x] ~~Q6 без стража `courier_id IS NULL`~~ — **исправлено аудитом** (код отставал от раздела 4 документа). Перепроверить и принять правку.
- [x] ~~T2 отменяла доставленный заказ~~ — **исправлено аудитом** (стражи по О22 + возврат только при состоявшейся отмене). Перепроверить и принять правку.
- [x] ~~Q8 считает «продажами» позиции отменённых заказов~~ — **исправлено** (dml v1.2, одобрено командой): `JOIN customer_order … AND o.status <> 'cancelled'`; §11 документа обновлён фактическим выводом («Унаги маки» из топа исчез, «Филадельфия» 4→3).
- [ ] **Q4 (история заказов):** `items_total` не учитывает скидку по промокоду — либо переименовать колонку/уточнить комментарий, либо выводить и сумму оплаты из `payment`.
- [ ] Индексы: при правке схемы Ролью 1 заодно добавить `address(customer_id)` (FK без индекса) и обсудить `payment(status, paid_at)` — раздел 4 НФТ его уже «рекомендует», но в `schema.sql` его нет.
- [ ] Прогнать весь `dml.sql` построчно после правок (`psql -v` для параметров), обновить заявленные выводы в разделе 11 документа.

## Роль 4 — AI-координатор и редактор

- [x] ~~`ai_log.md` содержит только ОБРАЗЦЫ~~ — образцы удалены, записан **первый реальный диалог** (агентная сессия аудита 2026-06-10; артефакты проверяемы по diff и пометкам `[аудит]` в SQL). 🔥 **Продолжать пополнение:** нужно ещё 2–3 диалога с разных этапов — разделы 13–15 документа всё ещё пусты, это до 6 баллов (№8 + №9).
- [ ] Провести **независимую верификацию второй LLM** (каркас запроса — §2.2 задания): отдать разделы 2–12 + schema.sql + dml.sql. Результаты — в раздел 14, разбор команды — в раздел 15. Подсказка: дыры из этого TODO (категория чужого ресторана, адрес чужого клиента, 150 % скидка, Q8 с отменёнными) — хорошая проверка качества рецензента: настоящая «вторая модель» должна найти хотя бы часть из них.
- [ ] Из 7 черновиков `decisions.md` выбрать ≥3 сильнейших и оформить раздел 16 (сильнейшие: №3 снимок цены, №6 составной FK, №7 отдельный `payment`).
- [ ] Титульный лист (раздел 1): ФИО, роли, дата.
- [ ] **Финальная сборка: сдаётся ОДИН файл** — встроить код `schema.sql` и `dml.sql` в разделы 10–11 (сейчас там только ссылки на файлы; внешние файлы в сдачу не попадут). Mermaid → заменить/продублировать PNG для PDF/DOCX.
- [ ] Закоммитить текущие незакоммиченные правки «Итогового документа» и работать мелкими коммитами.
