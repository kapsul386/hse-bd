# Платформа доставки еды «Перекус» — проектирование реляционной БД

**Курс:** Конструирование баз данных
**Формат:** групповой проект, 4 участника
**Домен:** доставка еды, маркетплейс ресторанов
**Дата финальной сборки:** 12 июня 2026

---

## 1. Титульный лист

**Название проекта:** реляционная база данных платформы доставки еды «Перекус».

**Состав и роли команды:**

| Участник | Роль в проекте | Зона ответственности |
|---|---|---|
| Участник 1 | Архитектор схемы | ER-модель, DDL, ограничения целостности |
| Участник 2 | Аналитик нормализации | зависимости, нормальные формы, анализ аномалий |
| Участник 3 | SQL-разработчик | DML-запросы, транзакции, тестовые данные |
| Участник 4 | AI-координатор и редактор | AI-приложение, спорные решения, финальная сборка |

Фактические ФИО участников подставляются командой при сдаче в учебную систему; техническое и аналитическое содержание документа финализировано.

---

## 2. Описание предметной области

**«Перекус»** — платформа-маркетплейс, соединяющая клиентов, рестораны и курьеров: клиент выбирает блюда из меню ресторанов, оформляет и оплачивает заказ, курьер доставляет его по адресу клиента.

**Кому нужна и кто пользуется:**
- **Клиент** — ищет рестораны, собирает заказ, платит, отслеживает статус, смотрит историю.
- **Ресторан** (менеджер) — ведёт меню и цены, видит входящие заказы и аналитику продаж.
- **Курьер** — получает назначенные заказы, отмечает забор и доставку.
- **Оператор платформы** — следит за заказами, курьерами, метриками сервиса.

**Ключевые пользовательские сценарии:**
1. Клиент оформляет и оплачивает заказ из одного ресторана → заказ уходит в готовку → назначается курьер → доставка.
2. Ресторан меняет цену/доступность блюда; цены в уже оформленных заказах не меняются.
3. Клиент отменяет заказ до определённого статуса; при оплаченном — возврат.
4. Ресторан/оператор смотрит аналитику: топ блюд, выручка, среднее время доставки.

Граница проекта: система моделирует именно БД-часть маркетплейса. Внешние интеграции с банками, геокодированием, мобильными push-уведомлениями и реальной маршрутизацией курьеров не моделируются; в БД фиксируются только сущности и события, нужные для заказов, меню, оплаты, доставки, отзывов и аналитики.

---

## 3. Функциональные требования

| № | Требование | Тип операций |
|---|---|---|
| ФТ-1 | Регистрация клиента и управление его адресами доставки | C/R/U/D |
| ФТ-2 | Просмотр ресторанов и их меню по категориям (только доступные блюда) | R |
| ФТ-3 | Формирование заказа из блюд **одного** ресторана | C |
| ФТ-4 | Расчёт суммы заказа по ценам на момент оформления, с учётом промокода | R |
| ФТ-5 | Оплата заказа (карта / наличные / СБП) | C/U |
| ФТ-6 | Назначение курьера и смена статуса заказа | U |
| ФТ-7 | Отмена заказа с возвратом оплаты (если оплачен) | U |
| ФТ-8 | История заказов клиента | R |
| ФТ-9 | Управление меню рестораном (добавить/изменить/снять блюдо, изменить цену) | C/R/U/D |
| ФТ-10 | Аналитика ресторана: топ блюд, выручка по дням | R (сложн.) |
| ФТ-11 | Аналитика платформы: активные курьеры, среднее время доставки, доля отмен | R (сложн.) |
| ФТ-12 | Оценка заказа клиентом (рейтинг ресторана/курьера) | C/R |

---

## 4. Нефункциональные требования

**Производительность.** Базовые `JOIN`-ы и фильтры опираются на индексы из `schema.sql`: по внешним ключам `customer_order(customer_id / restaurant_id / courier_id)`, по `order_item(item_id)`, по `menu_item(restaurant_id, category_id)`, по адресу клиента `address(customer_id)`; история и аналитика по датам — по `customer_order(created_at)`. Под тяжёлые аналитические отчёты (Q9 «выручка по дням», Q12 «доля отмен», Q14 «средний рейтинг») при росте объёмов основной приём — **материализованные представления** с `REFRESH` по расписанию и/или **партиционирование** `customer_order`/`payment` по дате; точечные индексы по статусу здесь малополезны (статус низкоселективен — `paid`/`delivered` покрывают большинство строк, планировщик предпочтёт Seq Scan), кроме **частичных** индексов под редкие значения — в схеме оставлен `idx_order_cancelled ON customer_order(restaurant_id) WHERE status = 'cancelled'` под Q12. Узкие места выявляются через `EXPLAIN ANALYZE` (B-Tree vs Seq Scan). _(Уточнено по замечанию Z10 рецензии, раздел 14.)_

**Многопользовательский доступ и конкуренция.** Критична гонка при назначении курьера: два оператора не должны назначить разных курьеров одному заказу. Решение — атомарный `UPDATE` со стражем (`… WHERE order_id = :id AND status IN ('paid','cooking') AND courier_id IS NULL`, Q6): первый выигрывает, второй меняет 0 строк. При строгой сериализации — `SELECT … FOR UPDATE` по заказу. Для T1 границы согласованности под конкуренцией разобраны в разделе 12 (остаточная гонка «снятие блюда ↔ оформление» и выбор уровня изоляции).

**Согласованность данных.** Ссылочная целостность — внешними ключами; доменная — `CHECK`/`UNIQUE`/`enum` (раздел 6). Ключевые денежные инварианты закрыты **декларативно**: «способ оплаты принимается рестораном» (О24) и «оплата привязана к ресторану своего заказа» — составными FK на `payment` (v4); «оплата = позиции − скидка» (О19) считается атомарно в транзакции оформления (раздел 12). Порядок меток времени и согласованность их со статусом (О23, частично О22) — табличными `CHECK` на `customer_order` (v4).

**Журналирование / аудит.** Базовый аудит встроен метками `created_at`, `paid_at`, `delivered_at`. Полная история переходов статусов — опциональная таблица `order_status_log(order_id, status, changed_at, actor)` на триггере (вынесено в расширение, чтобы не усложнять ядро).

**Масштабирование.** Партиционирование `customer_order` и `payment` по дате (`created_at`), вынос аналитики на реплику для чтения, материализованные витрины под отчёты ресторанов/платформы.

**Сопровождаемость.** Единый источник правды — `schema.sql` (schema freeze, правит Роль 1); `enum`-типы и говорящие имена; переносимость скриптов (`SET client_encoding`); воспроизводимые данные (`seed.sql`).

---

## 5. Предварительная модель данных

Итоговая модель — **14 сущностей** (v3: добавлен `order_review` под ФТ-12; v4: защита `payment` составными FK + CHECK-инварианты времени/статусов). Полный DDL: `schema.sql` (проверен на PostgreSQL 18.3/18.4). ER-диаграмма — ниже (Mermaid; рендерится прямо в GitHub) и как изображение [`er_diagram.png`](er_diagram.png) (для PDF/DOCX-версии документа). Исходник для редактирования — `schema.dbml` → dbdiagram.io.

### 5.1. Каталог сущностей

| Сущность | Назначение | Первичный ключ | Внешние ключи |
|---|---|---|---|
| `customer` | клиент | `customer_id` | — |
| `address` | адрес доставки клиента | `address_id` | `customer_id` |
| `restaurant` | ресторан-партнёр | `restaurant_id` | — |
| `cuisine` | справочник кухонь | `cuisine_id` | — |
| `restaurant_cuisine` | кухни ресторана (M:N) | `(restaurant_id, cuisine_id)` | `restaurant_id`, `cuisine_id` |
| `restaurant_payment_method` | принимаемые способы оплаты | `(restaurant_id, method)` | `restaurant_id` |
| `menu_category` | категория меню | `category_id` | `restaurant_id` |
| `menu_item` | блюдо | `item_id` | `restaurant_id`, `category_id` |
| `courier` | курьер | `courier_id` | — |
| `promo_code` | промокод | `promo_id` | — |
| `customer_order` | заказ | `order_id` | `customer_id`, `restaurant_id`, `address_id`, `courier_id?`, `promo_id?` |
| `order_item` | позиция заказа (M:N заказ↔блюдо) | `(order_id, item_id)` | →`customer_order(order_id,restaurant_id)`, →`menu_item(item_id,restaurant_id)` |
| `payment` | оплата заказа (1:1) | `payment_id` | `order_id` (UNIQUE); →`customer_order(order_id,restaurant_id)`, →`restaurant_payment_method(restaurant_id,method)` |
| `order_review` | отзыв о выполненном заказе (0..1 к заказу) | `review_id` | `order_id` (UNIQUE) |

`?` — необязательный (NULLable) внешний ключ.

### 5.2. Связи и кардинальности
- `customer` 1—N `address`; `customer` 1—N `customer_order`
- `restaurant` 1—N `menu_category` 1—N `menu_item`
- `restaurant` N—M `cuisine` (через `restaurant_cuisine`); `restaurant` 1—N `restaurant_payment_method`
- `customer_order` 1—N `order_item` N—1 `menu_item` ⇒ заказ ↔ блюдо как **M:N**
- `customer_order` 1—(0..1) `payment`; `customer_order` 1—(0..1) `order_review`; `courier` 1—N `customer_order` (у заказа 0..1 курьер); `promo_code` 1—N `customer_order` (0..1 промокод)
- `address` 1—N `customer_order` (с v3 — составной FK: адрес принадлежит клиенту заказа)

### 5.3. ER-диаграмма

![ER-диаграмма проекта «Перекус»](er_diagram.png)

> Версия с подписями связей (рендерится в GitHub из исходника ниже):

```mermaid
erDiagram
    customer ||--o{ address : "адреса"
    customer ||--o{ customer_order : "заказы"
    address ||--o{ customer_order : "доставка"
    restaurant ||--o{ menu_category : "категории"
    restaurant ||--o{ menu_item : "меню"
    restaurant ||--o{ customer_order : "заказы"
    restaurant ||--o{ restaurant_cuisine : "кухни"
    restaurant ||--o{ restaurant_payment_method : "оплаты"
    cuisine ||--o{ restaurant_cuisine : "рестораны"
    menu_category ||--o{ menu_item : "блюда"
    menu_item ||--o{ order_item : "позиции"
    customer_order ||--o{ order_item : "позиции"
    customer_order ||--o| payment : "оплата"
    restaurant_payment_method ||--o{ payment : "способ оплаты (О24)"
    customer_order ||--o| order_review : "отзыв"
    courier |o--o{ customer_order : "доставка"
    promo_code |o--o{ customer_order : "скидка"

    customer {
        bigint customer_id PK
        text full_name
        text phone UK
        text email
        timestamptz created_at
    }
    address {
        bigint address_id PK
        bigint customer_id FK
        text city
        text street
        text building
        text apartment
        text comment
    }
    restaurant {
        bigint restaurant_id PK
        text name
        text phone
        text address_text
        boolean is_active
    }
    cuisine {
        int cuisine_id PK
        text name UK
    }
    restaurant_cuisine {
        bigint restaurant_id PK
        int cuisine_id PK
    }
    restaurant_payment_method {
        bigint restaurant_id PK
        payment_method method PK
    }
    menu_category {
        bigint category_id PK
        bigint restaurant_id FK
        text name
    }
    menu_item {
        bigint item_id PK
        bigint restaurant_id FK
        bigint category_id FK
        text name
        text description
        numeric price
        boolean is_available
    }
    courier {
        bigint courier_id PK
        text full_name
        text phone UK
        text vehicle_type
        boolean is_active
    }
    promo_code {
        bigint promo_id PK
        text code UK
        discount_type discount_type
        numeric discount_value
        numeric min_order_amount
        date valid_from
        date valid_to
    }
    customer_order {
        bigint order_id PK
        bigint customer_id FK
        bigint restaurant_id FK
        bigint address_id FK
        bigint courier_id FK
        bigint promo_id FK
        order_status status
        timestamptz created_at
        timestamptz paid_at
        timestamptz delivered_at
    }
    order_item {
        bigint order_id PK
        bigint item_id PK
        bigint restaurant_id FK
        int quantity
        numeric unit_price
    }
    payment {
        bigint payment_id PK
        bigint order_id FK "UNIQUE, 1:1"
        bigint restaurant_id FK
        numeric amount
        payment_method method
        payment_status status
        timestamptz paid_at
    }
    order_review {
        bigint review_id PK
        bigint order_id FK "UNIQUE, 0..1"
        smallint restaurant_rating
        smallint courier_rating
        text comment
        timestamptz created_at
    }
```

**Решение по статусам (закрыто):** жизненный цикл заказа храним одним полем `customer_order.status` (enum `order_status`). Отдельная таблица-лог `order_status_log` — необязательное расширение (НФТ, Роль 3), в базовую модель не входит.

---

## 6. Текстовые ограничения на данные

Ограничения предметной области (О1…О27). Для каждого указано, **как обеспечивается**: декларативно средствами БД (`UNIQUE`/`FK`/`CHECK`/`enum`/составной FK) или процедурно (приложение/транзакция/триггер).

**Уникальность и идентификация**
- **О1.** Телефон клиента уникален. — *`UNIQUE(customer.phone)`*
- **О2.** Телефон курьера уникален. — *`UNIQUE(courier.phone)`*
- **О3.** Код промокода уникален. — *`UNIQUE(promo_code.code)`*
- **О4.** Название кухни уникально. — *`UNIQUE(cuisine.name)`*
- **О5.** Имя категории уникально в пределах ресторана. — *`UNIQUE(menu_category.restaurant_id, name)`*

**Структура и кардинальности**
- **О6.** Каждый заказ относится ровно к одному клиенту и одному ресторану. — *`FK NOT NULL`*
- **О7.** У заказа ровно один адрес доставки. — *`FK address_id NOT NULL`*
- **О8.** У заказа 0..1 курьер и 0..1 промокод. — *`FK`, NULL разрешён*
- **О9.** На заказ — не более одной оплаты (связь 1:1). — *`UNIQUE(payment.order_id)`*
- **О10.** Блюдо принадлежит одному ресторану и одной категории; категория — одному ресторану. — *`FK NOT NULL`*
- **О11.** Заказ содержит ≥ 1 позицию (пустых заказов нет). — *приложение/транзакция оформления*

**Доменная целостность (межтабличная)**
- **О12.** Все позиции заказа ссылаются на блюда **того же** ресторана, что и заказ. — *составной FK (`decisions.md` №6)*
- **О13.** Адрес доставки заказа принадлежит клиенту этого заказа. — *составной FK `customer_order(address_id, customer_id) → address(address_id, customer_id)` (v3; простым FK не выразить, составным — да, см. `decisions.md` №8)*
- **О14.** Курьер назначается не раньше статуса `paid`/`cooking`. — *`CHECK chk_order_courier_status` (декларативно, БД, v4: `courier_id IS NULL OR status IN ('paid','cooking','on_the_way','delivered')`) + страж направления перехода в приложении (`dml.sql` Q6)*
- **О15.** Блюдо добавляется в заказ, только если `is_available`. — *приложение (проверка при оформлении, T1)*

**Значения и согласованность сумм**
- **О16.** Количество в позиции > 0. — *`CHECK(quantity > 0)`*
- **О17.** Цены и суммы ≥ 0 (`price`, `unit_price`, `amount`, `discount_value`). — *`CHECK`*
- **О18.** `unit_price` фиксируется при оформлении и далее не меняется (снимок цены). — *транзакция T1; строки `order_item` не обновляются*
- **О19.** Сумма оплаты = Σ(`quantity` × `unit_price`) − скидка по промокоду. — *приложение (расчёт при оформлении)*
- **О20.** Промокод применим, только если сумма заказа ≥ `min_order_amount` и дата ∈ `[valid_from, valid_to]`. — *приложение*
- **О21.** `valid_to ≥ valid_from` у промокода. — *`CHECK`*

**Жизненный цикл и время**
- **О22.** Статусы заказа: `created → paid → cooking → on_the_way → delivered`; `cancelled` — из любого незавершённого. — *`enum order_status` + согласованность статуса с метками (`CHECK chk_order_status_time`: `status='paid' ⇒ paid_at`, `status='delivered' ⇒ delivered_at` и т.д., v4) + переходы стражами в приложении (Q6, Q15, T2). Полная защита графа переходов — триггер-машина состояний, вынесена в расширение*
- **О23.** `created_at ≤ paid_at ≤ delivered_at` (для заданных значений). — *табличный `CHECK chk_order_time_flow` на `customer_order` (v4; внутристрочные сравнения выразимы декларативно)*

**Оплата и отзывы (дополнено v2–v4 по итогам аудита 2026-06-10 и независимой верификации 2026-06-12)**
- **О24.** Способ оплаты заказа входит в принимаемые рестораном этого заказа. — *составной FK `payment(restaurant_id, method) → restaurant_payment_method(restaurant_id, method)` (v4 — декларативно, в т.ч. при записи в обход T1; до v4 — только `EXISTS` в T1, см. Z3 раздела 14)*
- **О25.** На заказ — не более одного отзыва. — *`UNIQUE(order_review.order_id)`*
- **О26.** Отзыв оставляет клиент этого заказа и только после доставки (`status = 'delivered'`). — *приложение (Q13 — стражи в `WHERE`)*
- **О27.** Оценка ресторана обязательна, оценка курьера опциональна; обе — целые 1..5; оценка курьера допустима только если курьер был назначен. — *`NOT NULL`/`CHECK` (диапазон) + приложение (Q13 — согласование с `courier_id`, см. Z9 раздела 14)*

> Декларативно (БД) закрыты О1–О10, О12, О13, О14 (v4), О16, О17, О21, О23 (v4), О24 (v4), О25; плюс потолок percent-скидки (≤ 100), «категория блюда — из того же ресторана» (v2), согласованность статуса с метками (v4, частично О22) и диапазоны/обязательность оценок (частично О27). Процедурно (приложение/транзакции) — О11, О15, О18–О20, О26, направление переходов статусов и правило «оценка курьера — только если курьер был назначен» (часть О27): их нельзя выразить простыми `CHECK`/`FK`, поэтому они вынесены в бизнес-логику (см. транзакции, раздел 12).

---

## 7. Зависимости

### 7.1. Функциональные зависимости и ключи по отношениям

| Отношение | Функциональные зависимости | Потенциальные ключи | НФ |
|---|---|---|---|
| `customer` | `customer_id →` остальные; `phone → customer_id` | `{customer_id}`, `{phone}` | BCNF |
| `address` | `address_id →` остальные | `{address_id}` | BCNF |
| `restaurant` | `restaurant_id →` остальные | `{restaurant_id}` | BCNF |
| `cuisine` | `cuisine_id → name`; `name → cuisine_id` | `{cuisine_id}`, `{name}` | BCNF |
| `restaurant_cuisine` | — (всё-ключевое) | `{restaurant_id, cuisine_id}` | 4НФ |
| `restaurant_payment_method` | — (всё-ключевое) | `{restaurant_id, method}` | 4НФ |
| `menu_category` | `category_id → restaurant_id, name`; `(restaurant_id, name) → category_id` | `{category_id}`, `{restaurant_id, name}` | BCNF |
| `menu_item` | `item_id →` остальные; `category_id → restaurant_id` | `{item_id}` | 2НФ \* |
| `courier` | `courier_id →` остальные; `phone → courier_id` | `{courier_id}`, `{phone}` | BCNF |
| `promo_code` | `promo_id →` остальные; `code → promo_id` | `{promo_id}`, `{code}` | BCNF |
| `customer_order` | `order_id →` остальные | `{order_id}` | BCNF |
| `order_item` | `(order_id, item_id) → quantity, unit_price`; `order_id → restaurant_id`; `item_id → restaurant_id` | `{(order_id, item_id)}` | 1НФ \* |
| `payment` | `payment_id →` остальные; `order_id → payment_id`; `order_id → restaurant_id` | `{payment_id}`, `{order_id}` | BCNF \* |
| `order_review` | `review_id →` остальные; `order_id → review_id` | `{review_id}`, `{order_id}` | BCNF |

\* **Контролируемая избыточность.** `menu_item`, `order_item` и (с v4) `payment` несут осознанно дублирующий `restaurant_id` ради декларативных составных FK (`decisions.md` №6, №10). У `menu_item` и `order_item` это **снижает** нормальную форму: `menu_item` нарушает 3НФ (`category_id → restaurant_id` — детерминант-не-ключ), поэтому достигает лишь **2НФ**; `order_item` нарушает 2НФ (частичная `order_id → restaurant_id`), поэтому достигает лишь **1НФ**. В `payment` деградации НФ **нет**: `order_id` — потенциальный ключ (`UNIQUE`), значит `order_id → restaurant_id` имеет детерминантом ключ и BCNF сохраняется — здесь `restaurant_id` лишь контролируемая избыточность без снижения НФ. В «чистом» виде (без `restaurant_id`) все три были бы в BCNF. Значение всегда согласовано составными FK (`payment.restaurant_id` = `order.restaurant_id`; `item → category → restaurant` = `order → restaurant`) — аномалий нет; колонка не вводит независимого факта, лишь материализует уже выводимый.

\*\* **`restaurant.address_text`** (отмечено по замечанию Z6 рецензии, раздел 14) — единое справочное текстовое поле адреса ресторана, **намеренно** не разложенное на компоненты, в отличие от адреса клиента (`address.city/street/building/...`). Причина асимметрии: адрес клиента участвует в логистике и переиспользуется между заказами (нужен поиск/нормализация по компонентам), адрес ресторана — статическая витринная строка для отображения. На нормальную форму это не влияет (`restaurant_id → address_text`, атрибут зависит от ключа), но в ER-каталоге поле теперь показано явно.

### 7.2. Многозначные зависимости (MVD) → основание для 4НФ
В предобъединённом профиле ресторана `(restaurant_id, cuisine_id, method)` факты «кухни» и «способы оплаты» **независимы**: `restaurant_id ↠ cuisine_id` и `restaurant_id ↠ method` — нетривиальные MVD, не сводящиеся к FD. Хранение в одной таблице даёт декартово произведение и аномалии ⇒ разнесено на `restaurant_cuisine` и `restaurant_payment_method` (4НФ, раздел 8).

### 7.3. Зависимости включения (внешние ключи)
Ссылочная целостность — 17 FK, в т.ч. составные: `address.customer_id ⊆ customer`, `menu_category.restaurant_id ⊆ restaurant`, `menu_item.restaurant_id ⊆ restaurant`, `menu_item.(category_id, restaurant_id) ⊆ menu_category(category_id, restaurant_id)` (v2), `customer_order.{customer_id, restaurant_id, courier_id, promo_id} ⊆` соответствующие ключи, `customer_order.(address_id, customer_id) ⊆ address(address_id, customer_id)` (v3, О13), `order_item.(order_id, restaurant_id) ⊆ customer_order(order_id, restaurant_id)`, `order_item.(item_id, restaurant_id) ⊆ menu_item(item_id, restaurant_id)`, `payment.(order_id, restaurant_id) ⊆ customer_order(order_id, restaurant_id)` (v4), `payment.(restaurant_id, method) ⊆ restaurant_payment_method(restaurant_id, method)` (v4, О24), `order_review.order_id ⊆ customer_order` (v3), `restaurant_cuisine.{restaurant_id, cuisine_id} ⊆ {restaurant, cuisine}`, `restaurant_payment_method.restaurant_id ⊆ restaurant` (полный список — `schema.sql`).

### 7.4. Полнота и непротиворечивость
Замыкание приведённого множества FD покрывает все неключевые атрибуты каждого отношения (каждый детерминируется своим ключом); противоречий (двух значений у одного детерминанта) нет. Множество минимально — выводимые/транзитивные FD в каноническую схему не внесены, кроме осознанной избыточности `restaurant_id` (п. 7.1).

---

## 8. Нормализация

> Идея: берём «сырое» универсальное отношение `ЗАКАЗ` и доводим до итоговой схемы, на каждом шаге фиксируя устраняемую аномалию. Многозначные факты ресторана — отдельной веткой (4НФ). Перечень FD, ключей и НФ по отношениям — в разделе 7; конкретный пример аномалии со строками — в разделе 9.

**Исходное «сырое» отношение (UNF).** Одна строка = один заказ; позиции — повторяющаяся группа:

```
ЗАКАЗ( order_id;
       created_at, status, delivered_at;
       customer_id, customer_name, customer_phone, customer_email;
       deliv_city, deliv_street, deliv_building, deliv_apartment;
       restaurant_id, restaurant_name, restaurant_phone;
       courier_id, courier_name, courier_phone, vehicle_type;
       promo_code, discount_type, discount_value, min_order_amount;
       pay_method, pay_amount, pay_status, paid_at;
       { item_id, item_name, category_name, menu_price, quantity, unit_price }* )  ← повтор. группа
```

**Шаг 1 → 1НФ** (атомарные атрибуты, нет повторяющихся групп). Разворачиваем позиции — одна строка = одна позиция. Первичный ключ: `(order_id, item_id)`.
- _Устранено:_ неатомарный список позиций.  _Осталось:_ вся «шапка» заказа дублируется в каждой строке.

Действующие FD (сокращённо):
```
(order_id, item_id) → quantity, unit_price
order_id      → created_at, status, delivered_at, customer_id, restaurant_id,
                courier_id, promo_id, address(deliv_*), pay_*
customer_id   → customer_name, customer_phone, customer_email
restaurant_id → restaurant_name, restaurant_phone
courier_id    → courier_name, courier_phone, vehicle_type
item_id       → item_name, category_id, menu_price, is_available, restaurant_id
category_id   → category_name, restaurant_id
promo_id      → promo_code, discount_type, discount_value, min_order_amount
```

**Шаг 2 → 2НФ** (нет зависимостей от *части* составного ключа `(order_id, item_id)`).
- атрибуты, зависящие только от `order_id` → выносим в **ЗАКАЗ**(order_id, …шапка…);
- атрибуты, зависящие только от `item_id` → выносим в **БЛЮДО**(item_id, …);
- зависящие от полного ключа остаются: **ПОЗИЦИЯ**(order_id, item_id, quantity, unit_price).
- _Устранено:_ цена/название блюда больше не повторяются в каждом заказе (правка цены — в одном месте); данные заказа — не в каждой позиции.

**Шаг 3 → 3НФ** (нет транзитивных зависимостей «неключ → неключ»).
- В **ЗАКАЗ**: `order_id → customer_id → данные клиента` ⇒ **КЛИЕНТ**; аналогично `restaurant_id` ⇒ **РЕСТОРАН**, `courier_id` ⇒ **КУРЬЕР**, `promo_id` ⇒ **ПРОМОКОД**. Вводим суррогат `address_id`: `order_id → address_id → поля адреса` ⇒ **АДРЕС**(address_id, customer_id, …).
- В **БЛЮДО**: `item_id → category_id → category_name` ⇒ **КАТЕГОРИЯ**(category_id, restaurant_id, name).
- _Устранено:_ смена телефона ресторана/клиента — в одной строке; исчезают аномалии вставки/удаления «последнего заказа» (раздел 9).
- **ПЛАТЁЖ**(payment_id, order_id, amount, method, status, paid_at) выделен отдельно — это **не** требование 3НФ (поля оплаты зависят от `order_id` напрямую), а проектное решение (отдельная сущность; `order_id` — альтернативный ключ, связь 1:1). С v4 в неё добавлена контролируемая избыточность `restaurant_id` ради составного FK для О24 (см. шаг 4 и `decisions.md` №10). См. `decisions.md` №7.

**Шаг 4 → BCNF** (каждый детерминант — потенциальный ключ).
- **КАТЕГОРИЯ**: два потенциальных ключа — `category_id` и `(restaurant_id, name)`; оба ключи ⇒ BCNF.
- **ПЛАТЁЖ**: `payment_id` и `order_id` — оба ключи ⇒ BCNF.
- Остальные отношения имеют единственный ключ-детерминант ⇒ BCNF.
- ⚠️ **Осознанное исключение (контролируемая избыточность).** Колонка `restaurant_id` намеренно оставлена в `menu_item`, `order_item` и (с v4) `payment` ради декларативных составных FK — «позиции/категория из ресторана заказа» (`decisions.md` №6) и «способ оплаты принимается рестораном» (О24, `decisions.md` №10). У `menu_item` и `order_item` это снижает НФ: `menu_item` нарушает 3НФ (`category_id → restaurant_id` — детерминант-не-ключ), достигая лишь 2НФ; `order_item` нарушает 2НФ (частичная `order_id → restaurant_id`), достигая лишь 1НФ. У `payment` НФ **не снижается**: `order_id` — потенциальный ключ, поэтому `order_id → restaurant_id` зависит от ключа и BCNF сохраняется. Значение всегда согласовано (`item → category → restaurant` = `order → restaurant`; `payment.restaurant_id` = `order.restaurant_id`), поэтому аномалий нет; в «чистом» виде (без `restaurant_id`) все три были бы в BCNF (см. раздел 7).

**Шаг 5 → 4НФ** (нет нетривиальных многозначных зависимостей).
Факты «кухни ресторана» и «принимаемые способы оплаты» **независимы**. В объединённом **РЕСТОРАН_ПРОФИЛЬ**(restaurant_id, cuisine, accepted_method) — всё-ключевое отношение (формально BCNF), но есть независимые MVD `restaurant_id ↠ cuisine` и `restaurant_id ↠ accepted_method`. Приходится хранить декартово произведение: у «Бургер Хаус» 2 кухни × 3 способа оплаты = **6 строк**; добавили способ оплаты — +2 строки.
- _Декомпозиция:_ **РЕСТОРАН_КУХНЯ**(restaurant_id, cuisine_id) и **РЕСТОРАН_ОПЛАТА**(restaurant_id, method) + словарь **КУХНЯ**(cuisine_id, name). Каждое несёт одну MVD ⇒ 4НФ.

**Итоговая схема (= `schema.sql` v4), 14 отношений:**
`customer`, `address`, `restaurant`, `cuisine`, `restaurant_cuisine`, `restaurant_payment_method`, `menu_category`, `menu_item`, `courier`, `promo_code`, `customer_order`, `order_item`, `payment`, `order_review`.

> `order_review` выделен отдельной таблицей по тем же соображениям, что и `payment`: это самостоятельное опциональное событие (0..1 к заказу) со своим временем; поля в `customer_order` означали бы NULL у всех неоценённых заказов (см. `decisions.md` №9). Все атрибуты зависят от ключей `review_id`/`order_id` напрямую — BCNF.

> Примечание: `order_item.unit_price` — **не** нарушение нормализации. Это самостоятельный факт (цена на момент заказа ≠ текущая `menu_item.price`), см. раздел 9 и `decisions.md` №3.

---

## 9. Пример аномалии в ненормализованной схеме

Возьмём «сырую» плоскую таблицу — одна строка на позицию заказа, всё в одном месте:

`order_flat(order_id, customer_name, customer_phone, restaurant_name, restaurant_phone, item_name, item_price, quantity)`

Пример наполнения (две позиции заказа №1 и одна — заказа №7, оба в «Пицца Рома»):

| order_id | customer_name | customer_phone | restaurant_name | restaurant_phone | item_name | item_price | qty |
|---|---|---|---|---|---|---|---|
| 1 | Анна Иванова | +7-900-000-0001 | Пицца Рома | +7-495-100-1001 | Маргарита | 500.00 | 3 |
| 1 | Анна Иванова | +7-900-000-0001 | Пицца Рома | +7-495-100-1001 | Кола 0.5 | 120.00 | 1 |
| 7 | Борис Петров | +7-900-000-0002 | Пицца Рома | +7-495-100-1001 | Маргарита | 550.00 | 2 |

`restaurant_phone` зависит от ресторана, `customer_phone` — от клиента (`order_id → customer → phone`, транзитивно), `item_name` — от блюда, а не от строки. Отсюда три аномалии:

- **Обновления:** «Пицца Рома» сменила телефон → править нужно в **каждой** из строк 1–3 (и во всех прочих строках её заказов). Пропустили одну — в базе два разных телефона у одного ресторана.
- **Вставки:** новое блюдо или новый ресторан нельзя завести, **пока по ним нет заказа** — нет строки без `order_id` (а `order_id` — часть ключа).
- **Удаления:** удалили единственный заказ ресторана → вместе с ним **исчезли** его название и телефон (потеря справочных данных).

Виден и источник проблемы — избыточность: телефон ресторана в примере повторяется трижды.

**Как решает нормализация.** Раскладываем (раздел 8) на `customer`, `restaurant`, `menu_item`, `customer_order`, `order_item`: каждый факт хранится один раз, заказ ссылается на сущности по ключам. Телефон ресторана меняется в одной строке `restaurant`; новое блюдо/ресторан заводится независимо от заказов; удаление заказа не задевает справочные данные.

> **Тонкость про `item_price`.** В `order_flat` он смешивает **два** факта — текущую цену меню и цену в конкретном заказе. В нормализованной схеме они разделены: текущая — `menu_item.price`, историческая — `order_item.unit_price` (снимок). В примере «Маргарита» у заказа 1 идёт по 500, а у заказа 7 — уже по 550: это **не** избыточность, а разные факты (`decisions.md` №3).

---

## 10. SQL DDL

Полный DDL — `schema.sql` (применяется на PostgreSQL 18 без ошибок; порядок: `schema.sql` → `seed.sql`). Ключевые решения реализации:

- **Суррогатные ключи** `BIGSERIAL`/`SERIAL` у всех основных сущностей — стабильны и не зависят от бизнес-данных; естественные ключи (`phone`, `cuisine.name`, `promo_code.code`) закреплены как `UNIQUE` (альтернативные ключи).
- **Перечислимые типы** (`order_status`, `payment_method`, `payment_status`, `discount_type`) вместо строк — контроль допустимых значений на уровне БД.
- **Целостность по внешним ключам:**
  - `address` и меню каскадно удаляются с владельцем (`ON DELETE CASCADE` от `customer`/`restaurant`);
  - `customer_order` ссылается на `customer`/`restaurant`/`address` с поведением по умолчанию `RESTRICT` — клиента/ресторан с заказами нельзя стереть;
  - `courier_id`, `promo_id` — NULLable (заказ без курьера/промокода допустим);
  - отзыв (`order_review`) каскадно удаляется с заказом.
- **Декларативное ограничение «позиции — из ресторана заказа»** (ограничение О12, раздел 6): в `order_item` добавлена колонка `restaurant_id`, целостность держат **два составных FK** — на `customer_order(order_id, restaurant_id)` и `menu_item(item_id, restaurant_id)`; для этого на обеих таблицах заведены составные `UNIQUE`. Без триггеров (см. `decisions.md` №6).
- **Декларативное ограничение «категория блюда — из того же ресторана»** (v2, по итогам аудита 2026-06-10): составной FK `menu_item(category_id, restaurant_id) → menu_category(category_id, restaurant_id)`. Без него блюдо могло ссылаться на категорию **чужого** ресторана (подтверждено негативным тестом), что ломало инвариант `item → category → restaurant`, на котором держится контролируемая избыточность `restaurant_id` (разделы 7–8).
- **Декларативное ограничение «адрес доставки — клиента заказа»** (v3, О13): составной FK `customer_order(address_id, customer_id) → address(address_id, customer_id)`; колонка `customer_id` в заказе уже была, цена решения — один составной `UNIQUE` на `address`. Тот же приём, что и выше (см. `decisions.md` №8).
- **Декларативная защита оплаты** (v4, по замечанию Z3 рецензии): в `payment` добавлена колонка `restaurant_id` и **два составных FK** — на `customer_order(order_id, restaurant_id)` (оплата принадлежит своему заказу) и на `restaurant_payment_method(restaurant_id, method)` (способ оплаты принимается рестораном, О24). Теперь оплата с непринимаемым методом невозможна **даже при записи в обход транзакции T1** (`decisions.md` №10).
- **Снимок цены:** `order_item.unit_price` фиксирует цену на момент заказа (см. раздел 9, `decisions.md` №3).
- **CHECK-ограничения:** `quantity > 0`, `price ≥ 0`, `amount ≥ 0`, `promo_code.valid_to ≥ valid_from`; с v2 — percent-скидка промокода не превышает 100 (`discount_type <> 'percent' OR discount_value <= 100`); с v3 — оценки в отзыве `BETWEEN 1 AND 5` (О27); с v4 (по замечанию Z4 рецензии) — на `customer_order` именованные `chk_order_time_flow` (порядок меток времени `paid_at ≥ created_at`, `delivered_at ≥ paid_at`), `chk_order_status_time` (согласованность статуса с метками: `status='paid' ⇒ paid_at`, `status='delivered' ⇔ delivered_at` и т.д.) и `chk_order_courier_status` (курьер только со статуса `paid`), а на `payment` — `chk_payment_paid_at` (`pending ⇔ нет paid_at`, `paid/refunded ⇔ есть`).
- **Связь 1:1** заказ↔оплата — через `payment.order_id UNIQUE`; связь 0..1 заказ↔отзыв — через `order_review.order_id UNIQUE` (О25).
- **Индексы** под внешние ключи и аналитику: `customer_order(customer_id/restaurant_id/courier_id/created_at)`, частичный `customer_order(restaurant_id) WHERE status='cancelled'` под Q12, `order_item(item_id)`, `menu_item(restaurant_id, category_id)`, `address(customer_id)`. Точечные индексы по низкоселективному `status` убраны осознанно (Z10, раздел 4).
- **Переносимость:** первой строкой `SET client_encoding TO 'UTF8'` — защита от авто-WIN1252 на Windows.

> **Schema freeze:** `schema.sql` помечен как v4 (каноническая; v2 = первые правки аудита, v3 = О13 + `order_review`, v4 = декларативное О24 + CHECK-и времени/статусов). Дальнейшие изменения — только через Роль 1.

---

## 11. SQL DML

Все запросы реализованы в `dml.sql` и проверены на PostgreSQL 18.3/18.4 + `seed.sql`.

**CRUD-запросы (Q1–Q7, Q13, Q15)** — покрывают создание / чтение / изменение / удаление:

| № | Операция | Что делает | ФТ | Пользователь |
|---|---|---|---|---|
| Q1 | CREATE | регистрация клиента | ФТ-1 | клиент |
| Q2 | CREATE | добавить адрес доставки | ФТ-1 | клиент |
| Q3 | READ | меню ресторана по категориям (только доступные) | ФТ-2 | клиент |
| Q4 | READ | история заказов: сумма позиций до скидки + фактическая оплата (`LEFT JOIN` — заказы без позиций/оплаты не теряются) | ФТ-8 | клиент |
| Q5 | UPDATE | изменить цену блюда (снимки в заказах не трогаются) | ФТ-9 | ресторан |
| Q6 | UPDATE | назначить курьера и сменить статус (страж по статусу) | ФТ-6 | оператор |
| Q7 | DELETE | удалить неиспользуемый адрес (защита `NOT EXISTS`) | ФТ-1 | клиент |
| Q13 | CREATE | оставить отзыв на доставленный заказ (стражи: свой заказ + `delivered`) | ФТ-12 | клиент |
| Q15 | UPDATE | ресторан принял заказ в готовку (переход `paid → cooking`) | ФТ-6 | ресторан |

**Сложные запросы (Q8–Q12, Q14)** — демонстрируют ценность схемы (`JOIN` / `GROUP BY` / подзапросы / агрегаты / оконные функции):

| № | Что делает | Ключевой приём | ФТ | Пользователь |
|---|---|---|---|---|
| Q8 | топ-5 блюд каждого ресторана (отменённые заказы исключены) | оконная `ROW_NUMBER() OVER (PARTITION BY …)` | ФТ-10 | ресторан |
| Q9 | выручка по дням с нарастающим итогом | оконная `SUM() OVER (ORDER BY day)` | ФТ-10 | ресторан/оператор |
| Q10 | среднее время доставки по курьерам | агрегаты + `JOIN`, `AVG(interval)` | ФТ-11 | оператор |
| Q11 | клиенты с тратами выше средней | подзапрос в `HAVING` | ФТ-11 | маркетинг |
| Q12 | доля отменённых заказов по ресторанам | условная агрегация `COUNT(*) FILTER` + `GROUP BY` | ФТ-11 | оператор |
| Q14 | средний рейтинг ресторанов по отзывам | `AVG` + `JOIN` + `GROUP BY` | ФТ-12/ФТ-11 | клиент/оператор |

> _Семантика охвата (осознанно):_ Q10/Q12/Q14 на `INNER JOIN` показывают только сущности со связанными строками — курьера без доставок, ресторан без заказов или без единого отзыва в выдаче нет (отсутствие строки ≠ нулевая метрика). Для Q12 это вдобавок снимает деление на ноль (ресторан без заказов не даёт `0/0`). В отличие от Q4 (история заказов), где терять заказ без позиций нельзя и потому применён `LEFT JOIN` (Z5), здесь аналитика «по тем, у кого есть данные» — намеренный выбор.

Проверенные выводы на `seed.sql`: Q8 — лидеры продаж: «Маргарита» 5 шт. («Пицца Рома»), «Калифорния» и «Филадельфия» по 3 («Суши Кит»), «Картошка фри» и «Чизбургер» по 3 («Бургер Хаус»); позиции отменённого заказа №8 не считаются — поэтому «Унаги маки» в топ не входит; Q9 — нарастающий итог 3540 → 6279 → 8229 → 9979 → 11379; Q11 — Борис 3670, Геннадий 3140, Анна 2620; Q12 — «Суши Кит» 33.3 %, остальные 0 %; Q13 — отзыв на недоставленный заказ даёт `INSERT 0 0` (страж О26), дубль отзыва бьётся об `UNIQUE` (О25); Q14 — «Суши Кит» 5.00 (1 отзыв), «Пицца Рома» 4.50 (2), «Бургер Хаус» 3.50 (2). Транзакция T1 проверена в обоих направлениях: успешное оформление с промокодом `WELCOME200` даёт сумму 1020 (1220 − 200) и заполняет `promo_id`; попытки с непринимаемым способом оплаты, с промокодом ниже минимальной суммы или с пустым/частичным после фильтра `is_available` заказом откатываются целиком (заказа-сироты не остаётся). Дополнительно: прямой `INSERT INTO payment` с методом, который ресторан не принимает, отвергается составным FK (О24, в обход T1). Полные тексты с пояснениями «ФТ / кто / зачем» — в `dml.sql`.

---

## 12. Описание транзакций

Атомарными делаем операции, меняющие несколько связанных таблиц, где частичное выполнение оставит данные несогласованными. Выделены две (код — `dml.sql`, часть C).

**T1 — Оформление и оплата заказа.** Объединяет: создание `customer_order` (с `promo_id`) → вставку `order_item` со **снимком** текущих цен → проверку, что вставились **все** запрошенные позиции → расчёт суммы со скидкой по промокоду (О19/О20) и создание `payment` → перевод заказа в `paid`. С v4 **О24** дополнительно защищено составным FK в `payment`: даже запись в обход T1 не сможет выбрать метод оплаты, который ресторан не принимает.
- *Границы:* от создания заказа до фиксации оплаты — единый бизнес-акт «оформить и оплатить».
- *Расчёт скидки (О19/О20, v1.4):* промокод применяется только если действует по датам и сумма ≥ `min_order_amount`; иначе расчётная скидка `NULL`, итоговая `amount` становится `NULL` и `NOT NULL` откатывает всю транзакцию — оформить заказ с недействующим промокодом по «полной» цене молча нельзя.
- *Единый принцип отката:* любое нарушение бизнес-правила обнуляет `amount` или бьётся об ограничение и откатывает T1 целиком: пустой **или частичный** после фильтра `is_available` заказ — расхождение `inserted_cnt ≠ requested_cnt` (О11/О15) → `amount` = `NULL`; непринимаемый способ оплаты (О24) → составной FK на `restaurant_payment_method` (v4); невалидный промокод (О20) → `NULL`. Заказ «`paid` без корректной оплаты» структурно невозможен.
- *Без транзакции:* деньги списаны, а заказа нет; заказ без позиций; промокод применён к несуществующему заказу; сумма посчитана по ценам, успевшим измениться между шагами.
- *Проверено (PG 18.3/18.4):* успешное оформление с `WELCOME200` → `amount` 1020, `promo_id` заполнен; откат при непринимаемом методе, при промокоде ниже минимума и при пустом/частичном заказе — без заказа-сироты.

**T2 — Отмена оплаченного заказа с возвратом.** Объединяет смену статуса на `cancelled` и пометку `payment` как `refunded`.
- *Без транзакции:* статус «отменён», но деньги не возвращены (или наоборот) — рассогласование «заказ ↔ оплата».

**Назначение курьера — вне T1 (решено).** Курьер появляется позже оплаты, поэтому это отдельный шаг (Q6), а не часть оформления. Его конкурентная безопасность держится не на длинной транзакции, а на атомарном `UPDATE` со стражем по статусу / `courier_id IS NULL` (раздел 4); включать его в T1 значило бы держать транзакцию открытой в ожидании свободного курьера.

*Уровень изоляции (уточнено по замечанию Z1 рецензии, раздел 14).* T1 выполняется на `READ COMMITTED` (по умолчанию). Снимок цены берётся атомарно в момент вставки позиций, поэтому гонки цен с Q5 нет: `unit_price` уже зафиксирован до расчёта `amount`. **Остаётся одна узкая гонка:** между чтением `menu_item.is_available = true` и фиксацией позиции ресторан может снять блюдо с продажи (О15) — тогда в оплаченный заказ попадёт уже недоступная позиция. Для учебной нагрузки мы **осознанно оставляем `READ COMMITTED`** (простота, отсутствие ретраев), фиксируя границу явно; промышленные варианты её закрытия — `SELECT … FOR SHARE` по строкам `menu_item` внутри T1 (заблокировать прайс-лист на время оформления) либо изоляция `REPEATABLE READ` с обработкой `serialization_failure` и ретраем. Это пример осознанно сохранённого компромисса (см. раздел 15).

---

## 13. Репрезентативные фрагменты работы с основной AI-моделью

Полный журнал — `ai_log.md`. Ниже три показательных фрагмента: во всех AI выступает **инструментом анализа и верификации**, а решение остаётся за командой; ценность — там, где мы спорили с моделью или ловили её на шаблонном/неполном ответе.

### Фрагмент 1 — нормализация: `unit_price` это дубликат или отдельный факт?

**Запрос.** В `order_item` храню `unit_price`, хотя цена есть в `menu_item.price`. Это нарушение нормализации? Может, убрать колонку и брать цену из `menu_item`?

**Ответ модели.** Да, это избыточность: `unit_price` дублирует `menu_item.price`, цена функционально зависит от `item_id` — храните её только в `menu_item` и берите через `JOIN`.

**Критический запрос.** Не согласен. Цена меняется со временем, а в заказе нужна цена **на момент покупки**. Убрав `unit_price`, я исказю историю чеков при любом изменении цены. Это всё ещё нарушение нормализации?

**Ответ модели.** Справедливо: цена-на-момент-заказа зависит от `(order_id, item_id)`, а не от `item_id`, — это самостоятельный атрибут, а не дубль. Нарушения нет, колонку оставляем.

**Комментарий.** _Гипотеза:_ `unit_price` — отдельный временной факт. _Полезное:_ точная формулировка «зависит от `(order_id, item_id)`». _Спорное:_ первый ответ шаблонный («дубль → убрать»), модель не учла временную природу цены, пока её не направили. Решение приняли сами (`decisions.md` №3).

### Фрагмент 2 — ограничение целостности: триггер или декларативно?

**Запрос.** Как в PostgreSQL гарантировать, что все позиции заказа ссылаются на блюда того же ресторана, что и сам заказ?

**Ответ модели.** Сделайте триггер `BEFORE INSERT/UPDATE` на `order_item`, проверяющий совпадение `menu_item.restaurant_id` с рестораном заказа.

**Критический запрос.** Триггер императивен и обходится при массовых операциях. Можно декларативно? Если добавить `restaurant_id` в `order_item`, а на `menu_item` — `UNIQUE(item_id, restaurant_id)` и на заказе `UNIQUE(order_id, restaurant_id)`, сработают два составных FK?

**Ответ модели.** Да: составные FK делают несовпадение ресторанов структурно невозможным, цена — одна доп. колонка.

**Комментарий.** _Гипотеза:_ ограничение выразимо без триггера. _Полезное:_ модель подтвердила корректность приёма и его границы. _Спорное:_ по умолчанию выдала «дефолтный» триггер, декларативный вариант нашли сами — AI использовали для верификации, а не генерации (`decisions.md` №6). Этот же приём затем переиспользован трижды (категория блюда, адрес клиента, защита оплаты).

### Фрагмент 3 — аудит: проверка заявления «проверено на PG 18»

**Запрос.** Проведи аудит репозитория против задания: карта файлов, готовность по разделам, технический аудит SQL и модели; не выдумывай содержимое, не подгоняй нормализацию.

**Ответ модели.** Не ограничился чтением: поднял PostgreSQL 18.4 в Docker, прогнал `schema.sql` → `seed.sql` → запросы и транзакции — задокументированные числа (Q9, Q11, Q12) совпали с фактическими. Негативными тестами нашёл три дыры: блюдо может ссылаться на категорию чужого ресторана; заказ — на адрес чужого клиента; промокод со скидкой 150 % вставляется без ошибки. Плюс два расхождения «код ↔ документ»: Q6 без стража `courier_id IS NULL` и T2, молча отменявшая доставленный заказ.

**Критический запрос.** Не верю на слово — докажи дыру с категорией негативным тестом: приведи конкретный `INSERT`, который сейчас проходит, хотя ресторан категории не совпадает с рестораном блюда. И раздели находки: что можно закрыть декларативно средствами БД, а что придётся оставить в приложении?

**Ответ модели.** `INSERT INTO menu_item(restaurant_id, category_id, …) VALUES (1, <category из ресторана 2>, …)` проходит, потому что FK ведёт только на `menu_category(category_id)` без ресторана. Декларативно закрываются: категория (составной FK `(category_id, restaurant_id)`), адрес заказа (О13) и потолок скидки (`CHECK`). Процедурно остаются «оценивает только клиент доставленного заказа» (О26) и полнота позиций в T1 — они зависят от изменяемого статуса и состояния других таблиц, простым `CHECK`/`FK` не выразить.

**Комментарий.** _Гипотеза:_ «проверено на PG 18» в шапках файлов — реальный прогон, а не декларация, и заявленные инварианты действительно держатся. _Полезное:_ метод негативных тестов (вставка заведомо некорректных данных и проверка, что БД её отвергает) — взяли как стандарт проверки ограничений; живой прогон вместо вычитки глазами. _Спорное:_ часть предложений (составной FK для О13, индекс по статусу) приняли не сразу — обсудили цену, часть отложили. Подробности — `ai_log.md`.

---

## 14. Независимая верификация второй моделью

Перед финальной сборкой материалы проекта (этот документ + `schema.sql` + `seed.sql` + `dml.sql`) отданы **второй LLM — Claude Opus 4.8** — в роли критически настроенного рецензента, с чистым контекстом и каркасом запроса из §2.2 задания. Это другая модель, чем основная рабочая; рецензент не видел `ai_log.md`/`decisions.md`/`TODO.md`, чтобы критика была независимой. Ниже её отчёт дословно.

> **Рецензент: Claude Opus 4.8** (независимый агент, чистый контекст; доступ только к документу, `schema.sql`, `seed.sql`, `dml.sql`)
>
> **Общая оценка.** Проект зрелый: модель доведена до 4НФ, межтабличные инварианты («позиция из ресторана заказа», «категория того же ресторана», «адрес клиента заказа») закрыты декларативно составными FK — сильное и аккуратное решение. Однако ряд бизнес-инвариантов держится «честным словом приложения» там, где это создаёт реальный риск рассинхронизации, а некоторые декларативные ограничения отсутствуют.
>
> **Z1 (критично). T1 и уровень изоляции.** Документ утверждает, что для T1 «`READ COMMITTED` достаточен». Гонка цен с Q5 действительно неопасна (`unit_price` снимается атомарно). Но остаётся узкая гонка: между чтением `is_available = true` и фиксацией позиции ресторан может снять блюдо — оно попадёт в оплаченный заказ недоступным. Альтернатива: `SELECT … FOR SHARE` по `menu_item` внутри T1 либо `REPEATABLE READ` с ретраем; в любом случае убрать категоричное «READ COMMITTED достаточен».
>
> **Z2 (критично). Промокод заявлен, но не реализован.** ФТ-4/О19/О20 обещают расчёт с учётом промокода, но код T1 считал `amount` без скидки и не писал `promo_id`; при этом seed показывает применённые скидки (заказ 2: 2120−200). Расхождение «документ/seed ↔ код». Альтернатива: реализовать расчёт в T1 либо честно понизить статус требования.
>
> **Z3 (критично). Нет декларативного контроля `payment`.** О19 и О24 держатся только кодом T1; любой `INSERT/UPDATE payment` в обход (Q-админка, импорт) запишет произвольный `amount` или непринимаемый метод. Альтернатива: составной FK `payment(restaurant_id, method) → restaurant_payment_method` плюс триггер-валидатор суммы.
>
> **Z4 (существенно). Жизненный цикл статусов не защищён.** `enum` не задаёт порядок; прямой `UPDATE … SET status='delivered'` из `created` пройдёт; О23 (`created_at ≤ paid_at ≤ delivered_at`) не подкреплён CHECK; переход `paid→cooking` в DML отсутствует вовсе. Альтернатива: табличные CHECK + машина состояний (триггер) + недостающий запрос перехода.
>
> **Z5 (существенно). Q4 теряет заказы без позиций.** `INNER JOIN order_item` скрывает из истории клиента заказ, по которому позиций ещё/уже нет. Альтернатива: `LEFT JOIN` + `COALESCE(SUM(...),0)`.
>
> **Z6 (существенно). `restaurant.address_text` есть в DDL, но отсутствует в ER и в каталоге FD**, и моделирует адрес ресторана единой строкой, тогда как адрес клиента разложен на компоненты. Несимметрично и рассинхронизировано с документом. Альтернатива: внести в ER с обоснованием либо разложить аналогично `address`.
>
> **Z7 (существенно). Семантика выручки Q9.** Возврат на следующий день меняет «выручку» прошлого дня в отчёте, снятом задним числом, — отчёт невоспроизводим. Альтернатива: явно определить gross/net и при net учитывать дату возврата.
>
> **Z8 (вкусовое). Цена контролируемой избыточности `restaurant_id`** — лишние UNIQUE-индексы и невозможность переноса блюда между ресторанами. Для учебного проекта приемлемо, но стоит отметить осознанно.
>
> **Z9 (вкусовое). `courier_rating` без проверки наличия курьера** — оценка курьера допустима на заказе с `courier_id IS NULL`. Альтернатива: CHECK/приложение «`courier_rating` ⇒ был курьер».
>
> **Z10 (вкусовое). Мелочи:** избыточность `TRUNCATE … RESTART IDENTITY` + ручные ID + `setval` в seed; расхождение в подсчёте FK; индексы по низкоселективному `status` из НФТ реально не помогут (лучше частичные или материализованные витрины).
>
> **Резюме приоритетов:** сначала Z2/Z3 (расхождение «заявлено/реализовано» и защита денег), затем Z1/Z4 (корректность под конкуренцией и целостность жизненного цикла), далее Z5–Z7 точечно, Z8–Z10 — упомянуть осознанность выбора.

---

## 15. Комментарий команды по работе с AI

**Что оказалось действительно полезным.** Самыми ценными были Z2 и Z3 — обе про разрыв «документ обещает ↔ код делает». При однопоточном прогоне они незаметны: seed «доказывал» работу промокодов, которых в транзакции не было, а денежные инварианты держались кодом T1 и рассыпались бы при любой записи в обход. Это ровно тот класс дефектов, который выгодно ловить чужой моделью с чистым контекстом, а не своим взглядом, замыленным авторством. Z4 и Z5 — точные и дешёвые.

**Где AI ошибался или был поверхностным.** В Z1 рецензент начал с переоценки: заявил «price drift» как критическую гонку, тут же сам признал, что снимок цены её снимает, и свёл проблему к узкой гонке по `is_available`. То есть первая формулировка была громче сути — мы оценивали уже скорректированную версию. Z7 (невоспроизводимость выручки) для учебного домена с фиксированным seed и без отложенных возвратов — теоретичен.

**Что изменили после проверки.** Реализовали: **Z2** — расчёт промокода в T1 (О19/О20, запись `promo_id`); **Z3** — два составных FK на `payment`, теперь О24 декларативна и при обходе T1; **Z4** — табличные CHECK порядка времени и согласованности статуса с метками + добавлен Q15 (`paid→cooking`); **Z5** — `LEFT JOIN` в Q4; **Z6** — `address_text` внесён в ER с обоснованием асимметрии; **Z9** — страж оценки курьера в Q13. Все правки перепроверены негативными тестами на PostgreSQL 18.4.

**Что сохранили вопреки AI и почему.** **Z1:** оставили `READ COMMITTED` для T1 — для учебной нагрузки это проще и без ретраев, а оставшуюся гонку по `is_available` честно описали в разделе 12 вместе с промышленными вариантами (`FOR SHARE` / `REPEATABLE READ`). **Z7:** оставили валовую выручку по `paid`, явно зафиксировав семантику в комментарии Q9, — net-выручка с датами возвратов выходит за объём учебного проекта. **Z8/Z10:** контролируемую избыточность `restaurant_id` и стиль seed сохранили осознанно — это документированный компромисс (раздел 7.1, `decisions.md` №6/№10), а индексы по статусу из НФТ переписали на материализованные витрины и частичные индексы (раздел 4). Грань «приняли / отклонили с обоснованием» для нас и есть главный результат верификации.

---

## 16. Спорные проектные решения

Полный журнал — `decisions.md` (10 решений). Ниже пять с реальным выбором; для каждого — альтернатива и почему отвергнута.

### Р1. Снимок цены в `order_item.unit_price` (осознанная «денормализация»)
- **Выбрано:** дублировать цену блюда на момент оформления.
- **Альтернатива:** хранить только ссылку на `menu_item` и брать текущую цену через `JOIN`.
- **Почему:** цена-на-момент-заказа — **другой факт**, чем текущая цена; без снимка чек исказится при любом изменении цены. Это не нарушение нормализации (атрибут зависит от `(order_id, item_id)`), а корректная фиксация исторического факта. Демонстрация — «Маргарита» в заказе 1 по 500, в заказе 7 по 550 (раздел 9).

### Р2. Декларативные межтабличные инварианты через составные FK вместо триггеров
- **Выбрано:** контролируемая колонка `restaurant_id` + составные FK для трёх инвариантов («позиция из ресторана заказа», «категория блюда того же ресторана» v2, «способ оплаты принимается рестораном» v4) и `customer_id` для «адрес клиента заказа» (v3).
- **Альтернатива:** триггеры `BEFORE INSERT/UPDATE`.
- **Почему:** декларативное ограничение нельзя обойти забытой проверкой или массовой операцией и оно дешевле триггера; цена — несколько составных `UNIQUE`. Осознанная плата — формальное нарушение НФ контролируемой избыточностью (`menu_item` 3НФ, `order_item` 2НФ), но значение всегда выводимо и согласовано, аномалий нет (раздел 7.1). Это решение особенно укрепила верификация: замечание Z3 закрыли ровно этим уже отработанным приёмом.

### Р3. Выделение `payment` (и `order_review`) в отдельные сущности
- **Выбрано:** `payment` 1:1 и `order_review` 0..1 — отдельными таблицами, а не полями заказа.
- **Альтернатива:** поля `amount/method/status/paid_at` (и `rating/comment`) прямо в `customer_order`.
- **Почему:** это самостоятельные события со своим жизненным циклом и временем; 3НФ их выноса не требует (зависят от `order_id` напрямую) — это проектный выбор. Поля в заказе означали бы NULL у всех неоплаченных/неоценённых заказов и смешение жизненных циклов. `UNIQUE(order_id)` даёт честную кардинальность 0..1.

### Р4. 4НФ: кухни и способы оплаты ресторана — две отдельные таблицы
- **Выбрано:** `restaurant_cuisine` и `restaurant_payment_method` раздельно.
- **Альтернатива:** одна таблица `(restaurant_id, cuisine, method)`.
- **Почему:** «кухни» и «способы оплаты» независимы — в объединённой таблице нетривиальные MVD `restaurant_id ↠ cuisine` и `restaurant_id ↠ method`, декартово произведение и аномалии 4НФ (у «Бургер Хаус» 2 кухни × 3 оплаты = 6 строк). Разнесение их устраняет (раздел 8).

### Р5. Уровень изоляции T1 — осознанно `READ COMMITTED`
- **Выбрано:** оставить `READ COMMITTED`, закрыв оставшуюся гонку по `is_available` документированием, а не кодом.
- **Альтернатива:** `SELECT … FOR SHARE` по `menu_item` в T1 или `REPEATABLE READ` с обработкой `serialization_failure` и ретраем.
- **Почему:** для учебной нагрузки простота и отсутствие ретраев важнее закрытия редкой гонки «снятие блюда ровно в момент оформления»; граница и промышленные варианты явно описаны (раздел 12). Решение **сохранено вопреки** замечанию Z1 рецензента — как осознанный компромисс, а не упущение.
